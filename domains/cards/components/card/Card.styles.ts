import { StyleSheet } from "react-native";

export const cardStyles = StyleSheet.create({
    wrapper: {
        backgroundColor: "#FFF",
        padding: 16,
        borderRadius: 16,
        width: "100%"
    }
})