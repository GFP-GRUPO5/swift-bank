import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cardButton: {
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
    gap: 16,
    backgroundColor: "#FFF",
    padding: 16,
    position: "relative",
    justifyContent: "center",
    borderRadius: 8,
  },
});