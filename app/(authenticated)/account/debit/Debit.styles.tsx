import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  accountTitle: {
    fontSize: 24,
    fontWeight: "600",
    marginBottom: 16,
  },
  balance: {
    fontSize: 20,
    marginBottom: 32,
  },

});