import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  notificationCard: {
    padding: 16,
    borderRadius: 4,
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
    marginBottom: 8,
  },
  dot: {
    height: 8,
    width: 8,
    borderRadius: 100,
    backgroundColor: "red",
  },
});