import { StyleSheet } from "react-native";

export const searchInputStyles = StyleSheet.create({
  wrapper: {
    position: 'relative',
  },
  input: {},
  iconWrapper: {
    position: 'absolute',
    right: 8,
  },
})