import { StyleSheet } from "react-native";

export const transactionStyles = StyleSheet.create({
    title: {
        fontSize: 24,
        fontWeight: 600,
        textAlign: 'center',
        marginBottom: 16,
    },
    input: {
        backgroundColor: '#FFF',
        borderWidth: 1,
        borderColor: '2c2c2c',
        padding: 8,
        fontSize: 18,
        borderRadius: 4,
    },
    button: {
        padding: 16,
        borderRadius: 24,
        marginBottom: 40,
    },
    textButton: {
        textAlign: 'center',
        color: '#FFF',
        fontWeight: 600,
        fontSize: 20,
    }
})