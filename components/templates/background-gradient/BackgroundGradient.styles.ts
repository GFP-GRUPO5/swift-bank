import { StyleSheet } from "react-native";

export const backgroundGradientStyles = StyleSheet.create({
    wrapper:{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        padding: 16,
        gap: 24
    }
})